import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Marca principal — Azul ─────────────────────────────────────
  static const Color azulPrimario   = Color(0xFF2563EB); // CTA principal
  static const Color azulOscuro     = Color(0xFF1D4ED8); // variante oscura
  static const Color azulClaro      = Color(0xFF3B82F6); // variante clara
  static const Color azulSuave      = Color(0xFFEFF6FF); // fondo tenue azul

  // ── Acento dorado — Recompensas ────────────────────────────────
  static const Color dorado         = Color(0xFFC9A84C);
  static const Color doradoOscuro   = Color(0xFFA8893C);

  // ── Verde — Solo para cantidades de dinero ─────────────────────
  static const Color verdePrimario  = Color(0xFF16A34A);
  static const Color verdeOscuro    = Color(0xFF15803D);

  // ── Fondos ────────────────────────────────────────────────────
  static const Color fondoPrincipal = Color(0xFFF0F4FF); // blanco azulado
  static const Color fondoCard      = Color(0xFFFFFFFF);
  static const Color fondoCardBorde = Color(0xFFE2E8F0);
  static const Color fondoElevado   = Color(0xFFFFFFFF);

  // ── Textos ────────────────────────────────────────────────────
  static const Color textoPrimario      = Color(0xFF0F172A); // slate-900
  static const Color textoSecundario    = Color(0xFF64748B); // slate-500
  static const Color textoDeshabilitado = Color(0xFFCBD5E1); // slate-300

  // ── Estados ───────────────────────────────────────────────────
  static const Color exito  = Color(0xFF16A34A);
  static const Color error  = Color(0xFFDC2626);
  static const Color alerta = Color(0xFFD97706);
  static const Color info   = Color(0xFF2563EB);

  // ── Secciones ─────────────────────────────────────────────────
  static const Color colorJuegos    = Color(0xFF7C3AED); // morado
  static const Color colorEncuestas = Color(0xFF2563EB); // azul marca
  static const Color colorVideos    = Color(0xFFDC2626); // rojo
  static const Color colorWallet    = Color(0xFF2563EB); // azul marca

  // ── Sombras ───────────────────────────────────────────────────
  static const Color sombra = Color(0x14000000);
}
